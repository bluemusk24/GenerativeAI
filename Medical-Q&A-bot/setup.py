from setuptools import find_packages, setup

setup(
    name = 'Medical Chatbot',
    version= '0.0.0',
    author= 'Emmanuel Eigebdion',
    author_email= 'emmanueleigbedion@gmail.com',
    packages= find_packages(),       # this makes the .py files in the src as the local packages
    install_requires = []

)